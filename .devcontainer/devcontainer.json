{
    "image": "flozi00/atra:latest",
    "runArgs": [
        "--gpus",
        "all",
        "--pull",
        "always",
        "--network=host",
        "shm_size",
        "4g"
    ],
    "customizations": {
        // Configure properties specific to VS Code.
        "vscode": {
            // Add the IDs of extensions you want installed when the container is created.
            "extensions": [
                "ms-python.vscode-pylance",
                "ms-python.python"
            ]
        }
    }
}